class AIChatConfig {
  /// Default base URL for the AI Worker.
  static const String defaultWorkerBaseUrl =
      'https://perfume-ai-chat-worker.qessa-prefume.workers.dev';

  const AIChatConfig._();
}
